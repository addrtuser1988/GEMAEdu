# Student Speaking Assessment Report (Minimal)

Run locally:

```bash
npm install
npm start
# open http://localhost:3000
```

This is a minimal prototype with one API endpoint at `/api/report` returning the student JSON located in `data/student.json`.
